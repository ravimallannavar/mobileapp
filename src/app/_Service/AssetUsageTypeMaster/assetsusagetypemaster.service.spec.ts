import { TestBed } from '@angular/core/testing';

import { AssetsusagetypemasterService } from './assetsusagetypemaster.service';

describe('AssetsusagetypemasterService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: AssetsusagetypemasterService = TestBed.get(AssetsusagetypemasterService);
    expect(service).toBeTruthy();
  });
});
